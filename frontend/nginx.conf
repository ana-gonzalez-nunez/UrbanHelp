server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index user.html;

    location / {
        try_files $uri $uri/ /user.html;
    }

    location /health {
        access_log off;
        return 200 "healthy\n";
        add_header Content-Type text/plain;
    }
}
